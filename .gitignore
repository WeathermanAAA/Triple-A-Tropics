# The IBTrACS CSV is huge (~110 MB) and is re-downloaded fresh by the
# GitHub Action on every run, so we don't need to check it in.
ibtracs.WP.list.v04r01.csv

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/

# MP4 animation pipeline. Frames are cached between CI runs via
# actions/cache; the build dir is staged and pushed to the orphan
# `mp4-artifacts` branch — neither belongs on main.
_frame_cache/
_mp4_build/
