name: Update SST

# OISST v2.1 has a ~24-hour latency from NOAA NCEI, so we update once
# per day well after that window. Previously scheduled at 13:00 UTC
# exactly, but GitHub silently drops top-of-hour cron runs during
# high-load windows. Offset to :17 and add a backup at :47. Concurrency
# group below dedupes if both fire.
on:
  schedule:
    - cron: "17 13 * * *"
    - cron: "47 13 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

# Only one SST job at a time. If a run is in progress when the next
# cron fires, queue the new one instead of racing.
concurrency:
  group: update-sst
  cancel-in-progress: false

jobs:
  regenerate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          # Minimal plotting stack — no cartopy. We draw coastlines from
          # the Natural Earth GeoJSON the tracks workflow already caches.
          # Pillow is needed by the animation step below for poster JPGs.
          pip install numpy matplotlib netCDF4 requests Pillow

      - name: Ensure Natural Earth basemap files exist
        run: |
          set -euo pipefail
          NE="https://raw.githubusercontent.com/nvkelso/natural-earth-vector/master/geojson"
          for f in ne_50m_coastline.geojson ne_50m_admin_0_countries.geojson \
                   ne_110m_coastline.geojson ne_110m_admin_0_countries.geojson; do
            if [ ! -f "$f" ]; then
              echo "--- Downloading $f ---"
              curl -fSL --retry 3 --retry-delay 10 -o "$f" "$NE/$f" || true
            fi
          done

      - name: Generate SST plots
        # Pulls ~280 NetCDF files total: ~45 OISST + ~235 CRW (CRW now
        # computes its own 1991–2020 baseline from historical SST files
        # rather than consuming NOAA's pre-baked SSTA product, so it
        # also fetches 30 years × 6 extra DOYs for the change-map and
        # running-mean window-center climatologies). Renders 108 PNGs
        # (18 regions × 3 variants × 2 products). Typically takes
        # 20–35 min on a GH-hosted runner.
        run: python generate_sst_plots.py

      # --------------------------------------------------------------
      # MP4 animation pipeline
      # --------------------------------------------------------------
      # The animator renders 90-day evolution clips for 18 regions × 4
      # core products (actual / anomaly / anomaly_gmr / crw_anomaly).
      # On warm runs only the newest day's frame is rendered for each
      # (region × product) — the rest are reused from the cache below.
      #
      # MP4s land on the orphan `mp4-artifacts` branch (see the push
      # step further down), NOT on main. ffmpeg is preinstalled on
      # ubuntu-latest so no extra apt step is needed.

      - name: Restore animation frame cache
        # Monthly key so the cache rotates and prunes itself. The
        # restore-keys fall-through means a fresh month inherits the
        # previous month's frames as a warm starting point — only
        # frames older than 100 days actually get re-rendered.
        uses: actions/cache@v4
        with:
          path: _frame_cache
          key: sst-mp4-frames-${{ github.run_id }}
          restore-keys: |
            sst-mp4-frames-

      - name: Generate SST MP4 animations
        # Cold start (empty cache) takes ~90–120 min: ~6,500 frame
        # renders at ~1 s each plus h.264 encode for 72 MP4s. Warm
        # runs do 72 new frames + 72 re-encodes ≈ 5–8 min total.
        # ffmpeg comes preinstalled on ubuntu-latest.
        run: python generate_sst_animations.py

      # Save the cache regardless of whether the orphan-branch push
      # below succeeds — the rendered frames are valuable even if a
      # later step fails, and re-rendering them is the expensive part.
      - name: Save animation frame cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: _frame_cache
          key: sst-mp4-frames-${{ github.run_id }}

      - name: Push SST MP4 artifacts to orphan branch (force-push, scoped)
        # Force-push is intentional and SAFE here:
        #   * The `mp4-artifacts` branch is purpose-built for rewrites —
        #     each workflow run rewrites only its OWN family directory
        #     (`sst/`) and leaves sibling families' dirs untouched.
        #   * No source code lives on this branch; it is a CDN-friendly
        #     publish surface for raw.githubusercontent.com.
        #   * `main` is never touched by this step.
        # If the branch doesn't exist yet (first SST run), it is
        # created as an orphan with just `sst/` + `README.md`.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eo pipefail
          ARTIFACTS_DIR=/tmp/mp4-artifacts
          rm -rf "$ARTIFACTS_DIR"
          mkdir -p "$ARTIFACTS_DIR"
          cd "$ARTIFACTS_DIR"

          git init -q
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote add origin \
            "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          # Pull the existing branch if it's there; otherwise start fresh
          # as an orphan so the very first SST run can bootstrap it.
          if git fetch --depth=1 origin mp4-artifacts 2>/dev/null; then
            git checkout -q -b mp4-artifacts FETCH_HEAD
          else
            echo "mp4-artifacts branch does not exist yet — creating it"
            git checkout -q --orphan mp4-artifacts
            git rm -rfq . 2>/dev/null || true
          fi

          # Wipe ONLY the SST family dir; preserve sibling families'
          # dirs (subsurface/, armor3d/) when those workflows start
          # writing to this branch in future PRs.
          rm -rf sst
          mkdir -p sst
          cp -r "${GITHUB_WORKSPACE}/_mp4_build/sst/." sst/

          # Refresh README only if missing — manual edits survive.
          if [ ! -f README.md ]; then
            cp "${GITHUB_WORKSPACE}/_mp4_build/README.md" README.md
          fi

          git add sst README.md
          if git diff --cached --quiet; then
            echo "No SST artifact changes to push."
            exit 0
          fi
          git commit -q -m "chore: refresh SST MP4 artifacts ($(date -u +'%Y-%m-%d %H:%M UTC'))"
          # Force-push is fine here — see step header comment.
          git push --force origin mp4-artifacts

      - name: Commit and push if anything changed
        run: |
          set -eo pipefail
          git config --local user.name  "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sst/*.png sst/sst_meta.json sst/crw_meta.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No SST changes to commit."
            exit 0
          fi
          git commit -m "chore: refresh SST maps ($(date -u +'%Y-%m-%d %H:%M UTC'))"

          # Rebase-and-push loop. This workflow takes 10+ minutes to run,
          # so commits on main from other sources (you, or the ACE
          # workflow) are very likely to land while we're rendering.
          # Instead of failing outright, fetch their work, replay our
          # commit on top, and push again. Our changes touch only
          # sst/*.png + sst/sst_meta.json, so conflicts are nearly
          # impossible in practice.
          for attempt in 1 2 3 4 5; do
            if git push; then
              echo "Pushed on attempt $attempt."
              exit 0
            fi
            echo "Push rejected on attempt $attempt — fetching + rebasing..."
            git fetch origin main
            git rebase origin/main || {
              echo "Rebase failed (unexpected conflict)." >&2
              git rebase --abort || true
              exit 1
            }
            sleep $((attempt * 2))
          done
          echo "Push still failing after 5 attempts." >&2
          exit 1
